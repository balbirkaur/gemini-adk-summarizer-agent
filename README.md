# gemini-adk-summarizer-agent
Problem Statement  Build and deploy a single AI agent using ADK and Gemini that is hosted on Cloud Run and performs one clearly defined task.  The agent must be callable via an HTTP endpoint and return a valid response for a given input.  This is a mini project focused on agent structure and deployment, not complex logic.
